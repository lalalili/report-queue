name: Release

on:
  push:
    tags: ['v*']

jobs:
  release:
    uses: lalalili/.github/.github/workflows/php-package-release.yml@main
    secrets: inherit
    with:
      php-extensions: 'dom, mbstring, pdo_sqlite, zip'
